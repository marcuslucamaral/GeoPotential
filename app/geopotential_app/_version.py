"""The application's version. One source, and this is it.

MAJOR.MINOR.PATCH with PATCH zero-padded to two digits, as
`../docs/VERSIONING.md` defines. PEP 440 strips the padding, so `pip` reports
`0.1.0` where the project form is `0.1.00`.
"""
VERSION = "0.8.12"
PEP440_VERSION = "0.8.12"
