"""The worker's version. One source, and this is it for the worker package.

The application package has its own; the two are reported side by side in the
handshake so a mismatched pair is visible rather than inferred.
"""
VERSION = "0.5.06"
PEP440_VERSION = "0.5.6"
